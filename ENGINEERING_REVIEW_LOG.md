# Engineering Review Log

This change log preserves material credibility defects, the corrections made, and the current artifact that verifies each outcome. It is engineering history, not an external review or endorsement.

| Round | Credibility issue | Repository change | Verifiable outcome |
| --- | --- | --- | --- |
| 1. Claim boundaries | Early project names and summaries implied deeper AI capability than the code supported. | Renamed weak systems, separated selected work from experiments, and added a claims policy plus automated phrase checks. | `scripts/check_claims.py` and `scripts/check_repo_health.py` run in full verification. |
| 2. AEC flagship depth | The original AEC RAG looked like a basic local demo. | Added metadata-rich chunking, four retrieval modes, citations, abstention, evaluation cases, failure analysis, architecture docs, and focused tests. | Synthetic 51-case regression plus public 24-case snapshot over 15 validated downloads. |
| 3. Public-source credibility | Public-document claims lacked fail-closed validation and reproducible identity. | Added source manifests, resolved URLs, PDF/content validation, SHA-256 fingerprints, and snapshot-scoped reporting. | Public `Hit@1 0.952`, `MRR 0.976`, paraphrase `MRR 0.917`, no-answer `1.000`. |
| 4. Embodied AI evidence | Language-to-action logic was initially too close to a scripted grid demo. | Added procedural splits, expert trajectories, random-forest behavior cloning, raw and filtered closed-loop evaluation, interventions, and failures. | The first bounded holdout exposed compounding policy errors and filter dependence. |
| 5. AEC workflow depth | Massing, communication, and QS ideas existed only as portfolio concepts. | Built three bounded systems with typed contracts, focused tests, evaluators, architecture docs, limitations, and generated visuals. | Massing baseline gap, 35-message specification regression, and 21-line QS/tender fixture evaluation. |
| 6. Recruiter hierarchy | Generic text, research, and MLOps exercises diluted the AEC specialization. | Promoted evidence-backed AEC workflow projects and moved generic systems to experiments. Rewrote README, profile, site, role map, and review paths. | Exactly five selected projects and fourteen experiments enforced by repository health checks. |
| 7. Cross-project credibility | A workflow diagram implied continuity without demonstrating that the specification, massing, and QS schemas actually exchanged data. | Added a separate deterministic integration contract with approved-requirement gating, field-level sources, cross-project invariants, schematic geometry provenance, generated traces, and rejection tests. | One labeled synthetic fixture records 5 approved requirements, 16 sourced fields, 96 generated candidates, and 7 priced takeoff lines; tender analysis remains explicitly not run. |
| 8. Embodied representation audit | The embodied project had only an engineered classical policy, while future visual-policy language could be mistaken for implemented capability. | Added a fully observable semantic-raster encoder, a deterministic MLP baseline on the same demonstrations and holdout, model-specific tests and cards, a generated comparison visual, and explicit non-perception boundaries. | Across 96 holdout scenarios, the random forest reaches `0.855` action accuracy and `0.698` filtered success; the raster MLP reaches `0.478` and `0.292`. The weaker neural result remains published. |
| 9. Egocentric observation and terminal-semantics audit | The world-frame raster showed that a neural label alone added little, but the project did not test observation alignment. Visual QA also exposed a timeout path that could misclassify nonterminal battery recovery as task success. | Added a 5x5 agent-centered encoder and third shared-split MLP; then restricted success to task-specific terminal events, penalized unnecessary charging, and added recovery/timeout regression tests before regenerating all evidence. | Egocentric action accuracy is `0.834`; raw success is `0.573`; corrected filtered success is `0.760` with `943` interventions. The discarded inflated result is not used on public surfaces. |
| 10. Evidence-presentation audit | A candidate-authored scorecard used numeric ratings and interview language that could be mistaken for an independent assessment. The claim checker also skipped static-site HTML and integration docs. | Replaced the scorecard with a source-linked coverage matrix, added explicit presentation rules, expanded scanning to Markdown and HTML, and added regression tests plus an obsolete-file invariant. | `scripts/check_claims.py` now audits 163 public text files; the evidence audit reports implemented scope and missing validation without self-ratings. |
| 11. Flagship service-boundary audit | The AEC project exposed FastAPI endpoints and local logs without authentication, readiness semantics, request tracing, service-level tests, or a reproducible contract artifact. Deployment recipes could be read as stronger evidence than the repository contained. | Added a fail-closed service factory, bounded request IDs, public liveness/readiness, default log redaction, process-local metrics, focused migration/error tests, and a deterministic in-process evaluator. Reframed hosting instructions as unverified options. | The checked-in synthetic ASGI run passes 12/12 contract checks and records 9 requests before the metrics response; no external deployment or traffic is claimed. |
| 12. Embodied visual-input audit | The embodied project compared state encodings but had no pixel-derived policy, so its visual and VLA boundaries remained entirely hypothetical. | Added a deterministic egocentric RGB renderer, appearance-augmented MLP training, a held-out work-light palette, raw/filtered rollouts, pixel artifacts, a model card, and schema/shift tests. | Standard RGB accuracy/filter success are `0.813`/`0.719`; unseen-palette results fall to `0.417`/`0.427`, with raw success `0.000` and 3,315 interventions. Pixels remain explicitly state-rendered, not camera evidence. |
| 13. Flagship reliability-evidence audit | The flagship exposed only process-local metrics, so restart durability and latency/error objectives were named as future work rather than exercised behavior. | Added bounded payload-free SQLite request telemetry, explicit insufficient/pass/fail query objectives, fail-open telemetry errors, a concurrent in-process workload, app reconstruction, focused tests, and generated evidence. | The fixed synthetic workload passes 14/14 checks: 48/48 responses return 200, zero server errors occur, P95 is at or below 500 ms, and all 48 query rows remain after app reconstruction. This is not network, sustained-load, uptime, or deployment evidence. |
| 14. Embodied command-boundary audit | Pixel-derived observation evidence still ended at a discrete grid transition, while the documented absence of physics left collision behavior entirely inferred from hand-authored rules. | Added a pinned MuJoCo dependency, continuous planar command adapter, bounded actuators, named rigid-contact telemetry, balanced learned-policy holdout subset, compact evidence artifacts, generated comparison visual, and geometry/contact/determinism tests. | Raw egocentric commands contact named rigid geometry on 51/150 movements; filtered egocentric and A* traces record 0 contacts across 148 and 98 movements. The evidence is explicitly limited to planar command replay, not a mobile-robot controller, ROS, hardware, or physical safety. |
| 15. Flagship statistical-uncertainty audit | Public retrieval headlines reported point estimates from 24 authored cases, and the sorted ablation table could be read as stronger mode-selection evidence than the sample supported. | Added deterministic Wilson and bootstrap intervals, paired mode comparisons over matching case IDs, mismatch/tie/small-sample tests, generated JSON/report/SVG artifacts, and claim-checked recruiter-facing caveats. | Public Hit@1 is `0.952 [0.773, 0.992]` over 21 answerable cases; no-answer is 2/2 with interval `[0.342, 1.000]`; hybrid-vs-BM25 MRR delta is `0.012 [0.000, 0.036]` and therefore inconclusive on this authored set. |
| 16. Retrieval-granularity audit | Document-level Hit@1 could be read as evidence that the supporting passage itself ranked first, while the public set had no auditable exact-chunk/page targets. | Added 21 candidate-authored exact-chunk targets, 18 page targets, corpus validation, unique repeated-heading chunk IDs, granular metrics, uncertainty intervals, per-case label audit artifacts, focused tests, and a generated comparison visual. | Document Hit@1 remains `0.952`; exact-target Hit@1 is `0.810` and MRR is `0.881`; page-target Hit@1 is `0.778`. The labels are explicitly not independent expert annotations. |
| 17. Collaborative-specification framing audit | The requirements system already used role-tagged messages and approval gates, but its public name did not make the client-architect-team communication workflow obvious and could be read as a generic AI specification generator. | Renamed the public display to Project Communication and Specification Assistant, documented the shared-conversation boundary, refreshed the real UI screenshot, and added a multi-party transcript regression test. | The focused suite passes 11/11 tests; messages retain sequence, role, and unique IDs while only role-authorized approvals produce draft clauses. The parser remains deterministic, synthetic-data-only, and neither open-domain chat nor a professional specification service. |
| 18. No-evidence experiment pruning | A public LoRA-named experiment contained a placeholder model identifier and a simulated run but loaded no tokenizer, model, adapter, or optimizer. Its honest caveats did not make it useful evidence, and it duplicated the stronger fitted text-classification lab. | Removed the experiment, its simulated artifacts, manifest and import paths, sample generator, and four configuration-only tests. Updated all experiment counts and added a repository-health invariant that prevents the removed slug from returning to the public manifest. | The repository now exposes 13 experiments and 19 importable modules; the retained Local Text Classification Lab fits and evaluates actual model parameters. No LoRA or transformer-training claim is made. |
| 19. Specification-language coverage audit | The communication assistant reported perfect extraction only on five workflow fixtures authored around its grammar, so its public evidence did not show paraphrase misses or false-positive controls. | Added a separate 33-case candidate-authored direct/paraphrase/negative stress set, case-level failure artifacts, a generated comparison visual, question/historical/rejection abstention, documented phrase variants, focused tests, and an in-app audit view. | Stress precision is `1.000`, recall `0.920`, F1 `0.958`, exact-case accuracy `0.939`, and negative-control accuracy `1.000`. Two number-word misses remain checked in; the set is explicitly not blinded, independent, or open-domain evidence. |
| 20. Experiment identity and signal audit | Five generic baselines added little hiring evidence, while three useful experiments still had slugs or module names that implied unimplemented deep learning, reinforcement learning, a guardrails platform, VLM reasoning, fine-tuning, or agent behavior. A stale role map also advertised multimodal/CV evidence that the retained code could not support. | Removed the five low-signal baselines; renamed the BIM rule checker, visual-provider contract, and fitted text-classification lab through directories, modules, commands, tests, and evidence paths; removed the unsupported CV role target; and added health checks that reject inflated or retired slugs. | The public inventory now contains 5 selected projects and 8 focused experiments across 14 importable modules. Full verification passes 184 tests, 111 generated-artifact idempotence checks, claim scanning over 157 public text files, and link checks over 148 public Markdown files. |
| 21. Retained-experiment identity audit | Four retained experiments had accurate public titles but broader slugs, package names, and internal labels: robot-task planner, site safety monitor, agentic research assistant, and MLOps registry. Those identities could imply motion control, certified monitoring, adaptive planning, or registry behavior absent from the code. | Renamed the directories and packages to grid route planning, telemetry rules, deterministic research workflow, and local model serving; changed agent trace APIs to workflow terminology; changed the local registry label to artifact storage; rewrote the three weakest READMEs around executable evidence; and added retired-name plus app-entrypoint regression tests. | Full verification passes 189 tests, 111 generated-artifact idempotence checks, formatting over 151 Python files, claim scanning over 157 public text files, link checks over 148 public Markdown files, and imports for all 14 modules. |
| 22. Advertised-command contract audit | Project commands, Streamlit titles, and API titles were documented but not checked as one contract. Three retained apps still used a deprecated Streamlit argument, the energy experiment retained a broader pipeline slug, and two CI workflows duplicated the same verifier. | Added manifest-driven tests for all 13 advertised app and test commands, exact app-title checks, nine API-title checks, and deprecated-API rejection; renamed the energy experiment through its directory, package, docs, and artifacts; standardized the flagship name; and consolidated verification into one read-only, manually dispatchable CI workflow. | Full verification passes 221 tests, 111 generated-artifact idempotence checks, formatting over 151 Python files, claim scanning over 157 public text files, link checks over 148 public Markdown files, and imports for all 14 modules. |
| 23. Recruiter-journey and documentation audit | The first README screen exposed statistical notation before explaining its significance, generic flowcharts made the architecture look shallower than the implementation, and public commands were not validated across the repository. A troubleshooting note also described implemented SQLite persistence as future work. | Rewrote the main path around plain-language evidence and honest boundaries; moved detailed metrics into optional evidence notes; replaced generic flows with an executed, data-backed AEC journey; added a non-mutating reviewer command, repository-wide documented-command validation, responsive navigation, and corrected persistence documentation. | `python scripts/reviewer_check.py` passes 157 focused tests; full verification passes 227 tests and reproduces 111 text artifacts; 151 commands resolve across 36 public files. Browser checks at a 390-by-844 viewport show no page-level horizontal overflow, while the detailed system map remains inspectable in its own scroll area. |
| 24. Selected-project visual-consistency audit | The repository landing page had a clear journey, but opening an individual selected project still exposed long introductory paragraphs and generic Mermaid diagrams that understated the implemented controls, evidence, and failure boundaries. Visual claims also lacked one repository-wide freshness contract. | Rewrote all five selected-project openings around a concise engineering question; generated a shared five-stage visual language from current JSON evaluation artifacts; replaced generic README and architecture diagrams; added accessible SVG metadata, explicit human-review boundaries, profile visual evidence, social metadata, and an automated visual-contract check. | Five 1400-by-840 project maps reproduce from current artifacts and appear within the first 20 README lines. No generic Mermaid blocks remain on selected architecture surfaces. Full verification passes 230 tests, 116 artifact-idempotence checks, formatting over 160 Python files, and the 160-test non-mutating reviewer path. |
| 25. Public-presentation availability audit | A polished static portfolio existed only inside the repository and on localhost, while the repository homepage field was empty. The experience therefore could not provide the public, visual entry point expected from a portfolio reference. | Published the verified `portfolio-site` subtree from a dedicated `gh-pages` branch, enabled HTTPS-enforced GitHub Pages, added canonical/Open Graph metadata and a favicon, and linked the live site from the root README, profile README, and reviewer guide. | `https://josiahsutd-stack.github.io/ai-portfolio/` returns HTTP 200 with the expected title and assets; the Pages API reports branch `gh-pages`, path `/`, public visibility, and HTTPS enforcement. Desktop rendering was inspected at 1440 by 1100; the existing true 390-by-844 responsive check records no page-level horizontal overflow. |
| 26. Public entry-point and portfolio-hub audit | The GitHub account had no profile README, bio, website, or intentional pin, while the Pages homepage did not yet act as a direct index into every selected system and bounded experiment. | Activated a dedicated public profile README, added the portfolio URL and LinkedIn contact path, pinned `ai-portfolio`, made the visual portfolio the first profile and repository link, and linked a current homepage preview. Reorganized the Pages home around a five-stage project map, visual selected work, capability evidence, a complete project index, contact routes, and generated-image disclosure. Added profile and site regression checks. | Anonymous profile HTML returns 200 and exposes the profile README, portfolio visual, bio, website, and pinned repository. Browser checks at 1440 pixels and a true 390-by-844 viewport show no horizontal overflow; the mobile menu expands within 390 pixels. The non-mutating reviewer path passes 164 tests; full verification passes 234 tests, 116 artifact-idempotence checks, formatting over 162 Python files, 157 public-text claim scans, and 151 documented commands. |
| 27. Visual case-study and professional-context audit | The homepage was polished, but two primary project cards still sent readers directly into raw repository folders, while the public site did not connect the AEC and AI work to the supplied professional delivery history. | Added complete visual case studies for AEC RAG, the Construction Embodied Agent Simulator, and the Constraint-Aware Massing Explorer. Each page exposes the problem, system map, artifact-backed metrics, retained failure or limitation, engineering decisions, exact local commands, source, evaluation, tests, and data boundary. Added a five-role professional timeline from the supplied Architecture + AI portfolio, upgraded every static page to shared navigation, and registered the new metrics in the evidence contract. | Desktop and true 390-by-844 browser checks show no page-level horizontal overflow; case-study system maps remain independently scrollable on mobile and the shared menu stays within the viewport. Full verification passes 238 tests, 116 artifact-idempotence checks, formatting over 163 Python files, claim scanning over 160 public text files, 161 documented commands across 39 public files, and 15 headline claims across 62 public surfaces. |

The audit intentionally preserves limitations and negative evidence. Perfect fixture scores are labeled as deterministic regression results, not general model-quality claims.
