# Demo Outputs

These files are deterministically generated from the bundled **synthetic** conversation fixtures:

```bash
python experiments/project-specification-copilot/evaluate_specification.py
```

The Markdown specification is a human-review draft. The SVG is generated from the same message, requirement, and clause records; neither file is a real project artifact.
