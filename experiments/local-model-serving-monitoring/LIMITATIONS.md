# Limitations

- Synthetic churn data only.
- No real customer, financial, retention, lending, employment, or access decision use.
- Local artifact files are not a production registry.
- SQLite prediction logs are local review scaffolding, not production observability.
- Drift is simulated with feature shifts, not measured from real delayed labels.
- Strong metrics are expected because the synthetic label rule is simple.
