# System Card

This local workflow uses bundled demo documents, deterministic tool calls, and an optional mock provider fallback. The evidence covers trace fields, citation presence, and an approval marker; it does not cover autonomous research or production authorization.
