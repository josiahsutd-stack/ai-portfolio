# Focused Tests

The executable tests live in [`../../../tests/test_massing_explorer.py`](../../../tests/test_massing_explorer.py) so the repository verifier can run the complete suite from one location.
