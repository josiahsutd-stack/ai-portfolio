# System Card

This demo validates a multimodal visual QA workflow with mock inference. It does not claim true visual understanding unless a real VLM provider is added. Outputs should be treated as schema and product-flow examples, not factual image analysis.

