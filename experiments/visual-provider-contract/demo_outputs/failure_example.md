# Failure Example

Mock mode can validate upload handling and response schema, but it cannot confirm whether an actual defect is visible.

Example question:

> Is the crack on the left beam structurally dangerous?

The correct local response should be:

- the mock provider cannot inspect the real image semantics;
- structural danger cannot be assessed from this workflow;
- a real vision model plus domain expert review would be required.
