# Test Coverage

The root suite in `tests/test_qs_takeoff_tender_analysis.py` covers shared-wall measurement, opening deductions, scale and geometry validation, rate provenance, missing-rate handling, tender exceptions, evaluation metrics, API errors, and generated SVG boundaries.
