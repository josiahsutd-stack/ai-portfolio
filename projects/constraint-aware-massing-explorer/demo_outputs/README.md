# Demo Outputs

All files in this directory are deterministically generated from the bundled **synthetic** site scenarios by:

```bash
python projects/constraint-aware-massing-explorer/evaluate_massing.py
```

The SVGs are programmatic views of evaluated candidate rectangles. They are not architectural renders or project records.
